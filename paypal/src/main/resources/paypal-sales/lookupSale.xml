<template name="lookupSale" xmlns="http://ws.apache.org/ns/synapse">
  <parameter name="id" description="String containing valid sales id of paypal"/>
  <parameter name="authorization" description="Authorization header value"/>
  <sequence class="sequence">
    <property name="uri.var.id" expression="$func:id"/>
    <payloadFactory media-type="json">
      <format>
        {}
      </format>
    </payloadFactory>
    <property name="messageType" value="application/json" scope="axis2" />
    <property xmlns:ns2="http://org.apache.synapse/xsd" xmlns:ns="http://org.apache.synapse/xsd" 
     	xmlns:soapenv="http://www.w3.org/2003/05/soap-envelope" name="Authorization" expression="$func:authorization"
     	scope="transport" type="STRING"/>
    <call>
      <endpoint>
        <http method="get" uri-template="{uri.var.paypalUrl}/v1/payments/sale/{uri.var.id}"/>
      </endpoint>
    </call>
  </sequence>
</template>