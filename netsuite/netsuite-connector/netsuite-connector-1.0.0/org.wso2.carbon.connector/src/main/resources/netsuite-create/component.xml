<?xml version="1.0" encoding="UTF-8"?>
<component name="netsuite-create" type="synapse/template">
  <subComponents>
    <component name="addList">
      <file>addList.xml</file>
      <description>Use to add one or more records into the system.</description>
    </component>
    <component name="attach">
      <file>attach.xml</file>
      <description>Use to attach another record or file to or from another record</description>
    </component>
    <component name="initializeList">
      <file>initializeList.xml</file>
      <description>Use to emulate the UI workflow by prepopulating fields on transaction line items with values from a related record.</description>
    </component>
  </subComponents>
</component>