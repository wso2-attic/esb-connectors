<template name="getAutocomplete" xmlns="http://ws.apache.org/ns/synapse">
	<parameter name="prefix" description="The matching string for the searchable fields in the specified models." />
	<parameter name="models" 
		description="The comma separated list of modelName:count which is searched and the number of results from each." />

	<sequence>
		<property name="uri.var.prefix" expression="$func:prefix" />
		<property name="uri.var.models" expression="$func:models" />

		<call>
			<endpoint>
				<http method="get"
					uri-template="{uri.var.apiUrl}/autocomplete/ranked?prefix={uri.var.prefix}&amp;models={uri.var.models}" />
			</endpoint>
		</call>

		<!-- Remove custom Headers from the API Response -->
		<header name="ETag" action="remove" scope="transport" />
		<header name="X-Runtime" action="remove" scope="transport" />
		<header name="X-Robots-Tag" action="remove" scope="transport" />
		<header name="X-XSS-Protection" action="remove" scope="transport" />
		<header name="X-UA-Compatible" action="remove" scope="transport" />
		<header name="X-Date" action="remove" scope="transport" />
		<header name="X-Content-Type-Options" action="remove" scope="transport" />
		<header name="P3P" action="remove" scope="transport" />
		<header name="Strict-Transport-Security" action="remove" scope="transport" />
		<header name="WWW-Authenticate" action="remove" scope="transport" />
		<header name="Cteonnt-Length" action="remove" scope="transport" />
		<header name="Status" action="remove" scope="transport" />

	</sequence>
</template>
