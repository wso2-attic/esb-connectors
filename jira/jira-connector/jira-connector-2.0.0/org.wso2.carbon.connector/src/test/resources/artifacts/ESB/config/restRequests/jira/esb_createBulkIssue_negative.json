{
   "uri": "%s(apiUrl)",
   "username": "%s(username)",
   "password": "%s(password)",
   "issueUpdates": [{
         "fields": {
            "project": {
               "id": "INVALID"
            },
            "summary": "%s(summary)",
            "issuetype": {
               "id": "%s(issueTypeId)"
            }
         }
      }

   ]
}