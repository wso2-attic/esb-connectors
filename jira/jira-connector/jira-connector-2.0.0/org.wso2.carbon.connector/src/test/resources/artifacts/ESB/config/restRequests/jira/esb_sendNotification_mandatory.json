{
   "uri": "%s(apiUrl)",
   "username": "%s(username)",
   "password": "%s(password)",
   "issueIdOrKey": "%s(issueIdOrKey)",
   "subject": "%s(notificationSubject)",
   "to": {
      "users": [{
         "name": "%s(notifyUser)"
      }]

   }
}