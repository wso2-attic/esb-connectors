{
   "uri": "%s(apiUrl)",
   "username": "%s(username)",
   "password": "%s(password)",
   "issueUpdates": [{
         "fields": {
            "project": {
               "key": "%s(project)"
            },
            "summary": "%s(summary)",
            "issuetype": {
               "id": "%s(issueTypeId)"
            }
         }
      }

   ]
}