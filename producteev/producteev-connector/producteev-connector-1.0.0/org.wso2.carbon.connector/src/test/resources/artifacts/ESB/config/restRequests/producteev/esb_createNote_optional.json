{
   "apiUrl": "%s(apiUrl)",
   "accessToken": "%s(accessToken)",
   "taskId":"%s(taskIdWithFileNote)",
   "message":"%s(noteMessageOpt)",
   "files":[
      {
         "id":"%s(fileId)"
      }
   ]
}