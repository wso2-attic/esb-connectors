{
   "apiUrl":"%s(apiUrl)",
   "apiToken":"%s(apiToken)",
   "startDate":"%s(addedDate)",
   "interval":"day",
   "amount":2,
   "userId":"%s(userId)",
   "fieldKey":"add_time",
   "pipelineId":"%s(pipeLineId)",
   "excludeDeals":0,
   "fields":["deals"]
}
