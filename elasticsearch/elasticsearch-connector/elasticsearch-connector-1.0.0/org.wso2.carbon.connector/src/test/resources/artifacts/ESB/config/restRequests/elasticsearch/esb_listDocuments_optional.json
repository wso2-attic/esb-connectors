{
	"apiUrl": "%s(apiUrl)",
  	"indexName": "%s(indexName)",
    "docs" : [
        {
            "_index" : "%s(indexName)",
            "fields": ["user","post_date"],
            "_id" : "%s(documentIdMandatory)"
        },
        {
            "_index" : "%s(indexName)",
            "fields": ["user","post_date"],
            "_id" : "%s(documentIdOptional)"
        }
    ]
}