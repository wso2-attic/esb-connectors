<template xmlns="http://ws.apache.org/ns/synapse" name="getUserFollowedBy">
    <parameter name="user_id" description="the user id"/>
    <sequence>
        <property name="uri.var.user_id" expression="$func:user_id"/>
        <property name="Accept-Encoding" action="remove" scope="transport"/>
        <payloadFactory media-type="xml">
            <format>
                <soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/">
                    <soapenv:Header/>
                    <soapenv:Body>
                    </soapenv:Body>
                </soapenv:Envelope>
            </format>
            <args/>
        </payloadFactory>
        <call>
            <endpoint>
                <http method="GET"
                      uri-template="https://api.instagram.com/v1/users/{uri.var.user_id}/followed-by?access_token={uri.var.access_token}"/>
            </endpoint>
        </call>
        <property name="messageType" value="application/json" scope="axis2"/>
    </sequence>
</template>
