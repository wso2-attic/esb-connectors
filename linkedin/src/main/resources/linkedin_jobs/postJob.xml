<template name="postJob" xmlns="http://ws.apache.org/ns/synapse">
  <parameter name="accessToken" description="Parameter Description 0"/>
  <parameter name="partnerJobId" description="Parameter Description 1"/>
  <sequence class="sequence">
    <property class="property" name="accessToken" expression="$func:accessToken"/>
    <property class="property" name="partnerJobId" expression="$func:partnerJobId"/>
    <payloadFactory media-type="xml">
      <format>
        <soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/">
          <soapenv:Header></soapenv:Header>
          <soapenv:Body></soapenv:Body>
        </soapenv:Envelope>
      </format>
    </payloadFactory>
    <call>
      <endpoint>
        <http method="get" uri-template="{uri.wso2.connector.url}/rest/api/{uri.var.id}"/>
      </endpoint>
    </call>
  </sequence>
</template>