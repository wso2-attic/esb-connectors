{
    "apiUrl": "%s(apiUrl)",
    "accessToken": "%s(accessToken)",
    "listId": "%s(listId)",
    "subscribers": [
        {
            "EmailAddress": "%s(subscribersMandatoryEmail)",
            "Name": "%s(subscribersNameMandetory)"
        }
    ]
}

