{
	"accessToken":"%s(accessToken)",
	"apiUrl":"%s(apiUrl)",
	"format":"json",
	"product": {
		"title": "%s(productTitle)",
		"product_type": "%s(productType)",
		"vendor": "%s(vendor)",
		"tags": "%s(tags)",
		"published": true
	}
}
