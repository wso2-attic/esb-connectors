{
   "apiUrl":"%s(apiUrl)",
   "username":"%s(username)",
   "longitude": "%s(longitude)",
   "latitude": "%s(latitude)",
   "responseType": "json",
   "maxRows": "%s(maxRows)",
   "radius": "%s(radius)"
}