<template name="searchMessage" xmlns="http://ws.apache.org/ns/synapse">
	<parameter name="messageId" description="The unique identifier of the message." />

	<sequence>
		<property name="uri.var.messageId" expression="$func:messageId" />

		<!-- If the responseType type is not XML, set the default value to JSON -->
		<filter source="get-property('uri.var.responseType')" regex="xml">
			<then>
				<header name="Accept" value="application/xml" scope="transport" />
			</then>
			<else>
				<header name="Accept" value="application/json" scope="transport" />
			</else>
		</filter>

		<call>
			<endpoint>
				<http method="get"
					uri-template="{uri.var.apiUrl}/search/message{uri.var.authParams}&amp;id={uri.var.messageId}" />
			</endpoint>
		</call>

		<!-- Remove custom header information in response -->
		<header name="Strict-Transport-Security" scope="transport" action="remove" />
		<header name="X-Frame-Options" scope="transport" action="remove" />
		<header name="X-XSS-Protection" scope="transport" action="remove" />

	</sequence>
</template>