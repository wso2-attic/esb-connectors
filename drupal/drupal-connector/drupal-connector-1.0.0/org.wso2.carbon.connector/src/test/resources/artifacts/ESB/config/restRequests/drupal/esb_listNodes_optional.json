{
   "apiUrl": "%s(apiUrl)",
   "userName": "%s(userName)",
   "password": "%s(password)",
   "responseType": "%s(responseType)",
   "page":"%(nodesPage)",
   "pageSize":"%s(nodesPageSize)",
   "fields":"%s(nodesFields)"
}
