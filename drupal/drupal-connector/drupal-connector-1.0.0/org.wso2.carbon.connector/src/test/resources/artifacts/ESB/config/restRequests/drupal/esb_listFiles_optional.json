{
   "apiUrl": "%s(apiUrl)",
   "userName": "%s(userName)",
   "password": "%s(password)",
   "responseType": "%s(responseType)",
   "page":"%s(filesPage)",
   "pageSize":"%s(filesPageSize)",
   "fields":"%s(filesFields)"
}
