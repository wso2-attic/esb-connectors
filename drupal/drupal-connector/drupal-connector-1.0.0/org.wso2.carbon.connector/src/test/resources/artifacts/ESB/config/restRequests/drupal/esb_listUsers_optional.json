{
   "apiUrl": "%s(apiUrl)",
   "userName": "%s(userName)",
   "password": "%s(password)",
   "responseType": "%s(responseType)",
   "page":"%(usersPage)",
   "pageSize":"%s(usersPageSize)",
   "fields":"%s(usersFields)"
}
