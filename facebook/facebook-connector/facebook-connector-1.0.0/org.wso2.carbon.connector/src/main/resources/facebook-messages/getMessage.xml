<template name="getMessage" xmlns="http://ws.apache.org/ns/synapse">
    <parameter name="messageId" description="The messageId"/>
    <parameter name="fields" description="The fields which belongs to an object"/>
    <sequence>
        <property name="uri.var.messageId" expression="$func:messageId"/>
        <property name="uri.var.fields" expression="$func:fields"/>
        <call>
            <endpoint>
                <http method="get"
                      uri-template="{uri.var.apiUrl}/{uri.var.apiVersion}/{uri.var.messageId}?access_token={uri.var.accessToken}&amp;fields={+uri.var.fields}"/>
            </endpoint>
        </call>
        <!-- Remove response custom header information -->
        <header name="X-FB-Debug" scope="transport" action="remove"/>
        <header name="X-FB-Rev" scope="transport" action="remove"/>
        <header name="WWW-Authenticate" scope="transport" action="remove"/>
    </sequence>
</template>