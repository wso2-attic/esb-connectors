<template name="getStatus" xmlns="http://ws.apache.org/ns/synapse">
    <parameter name="statusMessageId" description="The statusMessageId"/>
    <parameter name="connection" description="The connection which belongs to an object"/>
    <parameter name="fields" description="The fields which belongs to an object"/>
    <sequence>
        <property name="uri.var.statusMessageId" expression="$func:statusMessageId"/>
        <property name="uri.var.connection" expression="$func:connection"/>
        <property name="uri.var.fields" expression="$func:fields"/>
        <call>
            <endpoint>
                <http method="get"
                      uri-template="{uri.var.apiUrl}/{uri.var.apiVersion}/{+uri.var.statusMessageId}/{uri.var.connection}?access_token={uri.var.accessToken}&amp;fields={+uri.var.fields}"/>
            </endpoint>
        </call>
        <!-- Remove response custom header information -->
        <header name="X-FB-Debug" scope="transport" action="remove"/>
        <header name="X-FB-Rev" scope="transport" action="remove"/>
        <header name="WWW-Authenticate" scope="transport" action="remove"/>
    </sequence>
</template>